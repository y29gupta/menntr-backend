// import { assignPermissionsToRole } from "../services/role.service";

// export async function assignPermissionsHandler(request:any, reply:any) {
//   const { roleId, permissionIds, institutionId } = request.body;

//   await assignPermissionsToRole(
//     request.prisma,
//     roleId,
//     permissionIds,
//     institutionId
//   );

//   return reply.send({ success: true });
// }
